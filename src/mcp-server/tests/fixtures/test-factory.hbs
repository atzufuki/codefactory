---
name: simple_function
description: Creates a simple TypeScript function
outputPath: src/{{functionName}}.ts
params:
  functionName:
    type: string
    required: true
  params:
    type: array
    required: false
  returnType:
    type: string
    required: false
---
export function {{functionName}}({{#each params}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}){{#if returnType}}: {{returnType}}{{/if}} {
  // TODO: Implement {{functionName}}
}
