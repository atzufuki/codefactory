{
  "name": "codefactory",
  "displayName": "CodeFactory",
  "description": "Extraction-based code generation with smart sync. Includes syntax highlighting for .codefactory template files with YAML frontmatter, Handlebars templating, and multi-language template support (TypeScript, Python, Rust, Go, Java, C#, C++, HTML, CSS, etc.).",
  "version": "0.1.1",
  "publisher": "codefactory",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Programming Languages",
    "Other"
  ],
  "activationEvents": [
    "workspaceContains:**/.codefactory.json"
  ],
  "main": "./out/extension.js",
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/vscode": "^1.80.0",
    "typescript": "^5.0.0"
  },
  "contributes": {
    "commands": [
      {
        "command": "codefactory.sync",
        "title": "CodeFactory: Sync"
      }
    ],
    "keybindings": [
      {
        "command": "codefactory.sync",
        "key": "ctrl+shift+s",
        "mac": "cmd+shift+s",
        "when": "editorTextFocus"
      }
    ],
    "languages": [
      {
        "id": "codefactory",
        "aliases": ["CodeFactory Template", "Factory"],
        "extensions": [".codefactory"],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "codefactory",
        "scopeName": "source.codefactory",
        "path": "./syntaxes/codefactory.tmLanguage.json"
      },
      {
        "scopeName": "source.handlebars.injection",
        "path": "./syntaxes/handlebars.injection.json",
        "injectTo": [
          "source.codefactory",
          "source.ts",
          "source.yaml"
        ]
      }
    ]
  }
}
